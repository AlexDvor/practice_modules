import { fireEvent, render, waitFor } from '@testing-library/react'

describe('Form', () => {
	it.todo('should render Form with children')

	it.todo('should invoke the onSubmit callback')

	it.todo('should invoke the onSuccess callback')

	it.todo('invoke the onError callback')
})
