import { render, screen /* fireEvent */ } from '@testing-library/react'
import userEvent from '@testing-library/user-event'

const testPlaceholder = 'test placeholder'

describe('Input', () => {
	it.todo('should render the input')

	it.todo('should render the input with the correct type')

	it.todo('should render the input with the correct class names')

	it.todo('should render the input without a label')

	it.todo('should render the input with the correct label')

	it.todo('should render the input with the correct value')

	it.todo('should invoke the onChange callback')
})
