import { render, screen, fireEvent, act, waitFor } from '@testing-library/react'
import App from './App'

describe('App', () => {
	it.todo('should renderApp with form elements adn a title')
	it.todo('should render error message when form was submit with a weak password')
	it.todo('should render success message after successful submit')
})
